# RAG v2.0 Production Requirements
  # Force exact compatible versions for Python 3.14

  streamlit>=1.28.0
  anthropic>=0.7.0
  plotly>=5.15.0
  pandas>=2.0.0
  requests>=2.28.0

  # Force exact NumPy version that works with ChromaDB
  numpy==1.24.4

  # Use specific ChromaDB version compatible with Python 3.14
  chromadb==0.4.15

  # ML dependencies with compatible versions
  sentence-transformers>=2.2.0
  scikit-learn>=1.3.0
  faiss-cpu>=1.7.0

  # Force exact protobuf version to avoid conflicts
  protobuf==3.20.3

  # Additional dependencies
  typing-extensions>=4.5.0

  # Ensure compatible SQLite for ChromaDB
  pysqlite3-binary>=0.5.0
