# RAG v2.0 Production Requirements
  # Python 3.14 compatible with flexible versions

  streamlit>=1.28.0
  anthropic>=0.7.0
  plotly>=5.15.0
  pandas>=2.0.0
  requests>=2.28.0

  # NumPy compatible range
  numpy>=1.24.0,<1.27.0

  # Try older ChromaDB that installs better
  chromadb>=0.3.26,<0.4.0

  # Core ML dependencies
  sentence-transformers>=2.2.0,<2.4.0
  scikit-learn>=1.3.0,<1.4.0

  # Skip FAISS for now (often causes install issues)
  # faiss-cpu>=1.7.0

  # Protobuf in safe range
  protobuf>=3.19.0,<4.0.0

  # Additional dependencies
  typing-extensions>=4.5.0
